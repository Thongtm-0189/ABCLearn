@model List<Quiz>
@{
	string role = UserLogin.Instance.RoleID;
}
<!--

<!DOCTYPE html>
<html lang="en">

<head>
	@Html.Partial("_head")
</head>

<body>
	@Html.Partial("_navStart")
	@if (UserLogin.Instance.RoleID != "Lecturer")
	{
		<a href="@Url.Action("Index", "Home")" class="nav-item nav-link ">Home</a>
		<a href="@Url.Action("About", "Home")" class="nav-item nav-link">About US</a>
		<a href="@Url.Action("Contact", "Home")" class="nav-item nav-link">Contact</a>
	}
	<a href="@Url.Action("Course", "Home")" class="nav-item nav-link">Course</a>
	@Html.Partial("_navEnd")
	<!-- tab search dictionary -->
	<div class="container-fluid bg-primary mb-5 hero-header">
		<div class="container py-1">
			<div class="row justify-content-center" style="margin-top: 100px">
				<div class="col-lg-10 text-center">
					<h1 class="display-3 text-white animated slideInDown">Test your ability</h1>
				</div>
			</div>
		</div>
	</div>

	<!-- Quiz Start -->
	<div class="container">
		@if (UserLogin.Instance.RoleID != "Lecturer")
		{
			<table class="table table-striped text-center">
				<tbody>
					<form action="@Url.Action("SubmitQuiz","Quiz")" class="form-control justify-content-center" method="post">
						@foreach (var quiz in Model)
						{
						<tr>
							<table class="text-center table-striped table">
								<thead>
									<tr>
										<th>@quiz.Question</th>
									</tr>
								</thead>
								<tbody>
									<tr>
										<td>
											<input type="radio" class="form-check-input" name="answer[@(quiz.Id)]" value="@quiz.AnswerA" />
											<label class="form-check-label">
												A: @quiz.AnswerA
											</label>

											<input type="radio" class="form-check-input" name="answer[@(quiz.Id)]" value="@quiz.AnswerB" />
											<label class="form-check-label">
												B: @quiz.AnswerB
											</label>

											<input type="radio" class="form-check-input" name="answer[@(quiz.Id)]" value="@quiz.AnswerC" />
											<label class="form-check-label">
												C: @quiz.AnswerC
											</label>

											<input type="radio" class="form-check-input" name="answer[@(quiz.Id)]" value="@quiz.AnswerD" />
											<label class="form-check-label">
												D: @quiz.AnswerD
											</label>
										</td>
									</tr>
								</tbody>
							</table>
						</tr>
						}
					<tr>
						<input type="radio" id="checkSubmit" class="form-check-input" />
						<label class="form-check-label">
							<b>I was sure to submit the post</b>
						</label>
						<div id="btnSubmit" style="display: none" class="container justify-content-center">
							<input type="submit" class="btn btn-primary" value="Done" />
						</div>
					</tr>
					</form>
				</tbody>
			</table>
		}
		else
		{
			<div class="row">
				<p class="col-6">NOTE: You can edit it directly and press the Edit button to edit.</p>
				<div class="col-4"></div>
				<div class="col-2">
					<button type="submit" name="btnStudentAdmin" class="col-6 btn btn-primary" value="View">ADD NEW</button>
				</div>
			</div>
			<table class="text-center table-striped table">
				<thead>
					<tr>
						<th>Question</th>
						<th>AnswerA</th>
						<th>AnswerB</th>
						<th>AnswerC</th>
						<th>AnswerD</th>
						<th>CorrectAnswer</th>
						<th>ACTION</th>
					</tr>
				</thead>
				<tbody>
					@foreach (var quiz in Model)
					{
						<tr>
							<form>
								<td>
									<input type="text" class="" name="Question" value="@quiz.Question" />
								</td>
								<td>
									A:<input type="text" class="" name="answerA" value="@quiz.AnswerA" />
								</td>
								<td>
									B:<input type="text" class="" name="answerB" value="@quiz.AnswerB" />
								</td>
								<td>
									C:<input type="text" class="" name="answerC" value="@quiz.AnswerC" />
								</td>
								<td>
									D:<input type="text" class="" name="answerD" value="@quiz.AnswerD" />
								</td>
								<td>
									CA:<input type="text" class="" name="CorrectAnswer" value="@quiz.CorrectAnswer" />
								</td>
								<td>
									<div class="row">
										<button type="submit" name="btnStudentAdmin" class="col-6 btn btn-primary" value="View">Edit</button>
									<button type="submit" name="btnStudentAdmin" class="col-6 btn btn-primary" value="Delete" style="color: red">Delete</button>
									</div>
								</td>
							</form>
						</tr>
					}
				</tbody>
			</table>
		}
	</div>
	<!-- Quiz End -->
	@Html.Partial("_footer")
	@Html.Partial("_endScript")
	<script>
		var checkSubmit = document.querySelector("#checkSubmit");
		var tbnSubmit = document.querySelector("#btnSubmit");
		checkSubmit.addEventListener('click', (e) => {
			if (e.target.checked) {
				tbnSubmit.style.display = "block";
			} else {
				tbnSubmit.style.display = "none";
			}
		})
	</script>
</body>

</html>

-->
